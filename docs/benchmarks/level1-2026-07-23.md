# Level 1 real-data verification — 2026-07-23

## Environment

- macOS 26.5.2 / arm64
- Python 3.13.14
- Input root: legacy project `data/raw/20211221`
- Input JSON: not used

## Dataset

- 67 shot folders
- 810 `.hdr + .dat` pairs
- scan problems: 0
- selected verification series: `107845_032/3_3_01`
- selected series: 600,000 points

## Result

| Measurement | Result |
|---|---:|
| Folder scan | 0.180 s |
| One waveform read + calibration | 0.0016 s |
| Min/max display downsampling | 0.0114 s |
| Display points | 48,001 |
| Process peak RSS | 49.1 MB |

Times are single-run development-machine observations, not release guarantees.

## Legacy reader comparison

The same `3_3_01.hdr/.dat` pair was read with the legacy `pantaADC` and the Level 1 reader.

| Comparison | Maximum absolute difference |
|---|---:|
| Calibrated signal | 0.0 |
| Sampling time axis | 0.0 |

The new reader deliberately keeps the legacy one-based sampling-axis convention. The exact Yokogawa `HOffset` semantics remain a separate research task.

## Gate assessment

- Folder scan is below the Level 1 medium-data target of 5 seconds.
- First-series read plus display conversion is below 300 ms.
- All 810 pairs are recognized without JSON configuration.
- Peak RSS is low enough for this dataset, but repeated series switching must still be observed in the running GUI.
